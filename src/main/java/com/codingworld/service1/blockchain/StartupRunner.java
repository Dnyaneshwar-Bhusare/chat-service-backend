
package com.codingworld.service1.blockchain;
/*
import org.springframework.boot.CommandLineRunner;
import org.springframework.stereotype.Component;
import java.util.List;

@Component
public class StartupRunner implements CommandLineRunner {

    private final BlockchainService blockchainService;
    private final UserRepository userRepository;

    public StartupRunner(BlockchainService blockchainService, UserRepository userRepository) {
        this.blockchainService = blockchainService;
        this.userRepository = userRepository;
    }

    @Override
    public void run(String... args) throws Exception {
     //   List<String> ganacheAccounts = blockchainService.getAllGanacheAccounts();
     //   List<User> users = userRepository.findAll();
//
//        for (int i = 0; i < users.size() && i < ganacheAccounts.size(); i++) {
//            User user = users.get(i);
//            user.setEthereumAddress(ganacheAccounts.get(i));
//            userRepository.save(user);
//        }

        System.out.println("Assigned Ethereum addresses to all users successfully!");
    }
}
*/

public class StartupRunner {

}
